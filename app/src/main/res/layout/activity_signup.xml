<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center"
    android:background="@color/primary_background">

    <TextView
        android:id="@+id/singleTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Create Account"
        android:textColor="@color/text_primary"
        android:textSize="28sp"
        android:textStyle="bold"
        android:layout_marginBottom="32dp" />

    <TextView style="@style/TAAM_HeaderText" android:text="Email" android:layout_gravity="start" />
    <EditText
        android:id="@+id/signupEmailInput"
        style="@style/TAAM_EditText"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp"
        android:autofillHints="emailAddress"
        android:importantForAutofill="yes"
        android:hint="Enter your email"
        android:inputType="textEmailAddress" />
    
    <TextView style="@style/TAAM_HeaderText" android:text="Username" android:layout_gravity="start" />
    <EditText
        android:id="@+id/signupUsernameInput"
        style="@style/TAAM_EditText"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp"
        android:autofillHints="username"
        android:importantForAutofill="yes"
        android:hint="Choose a username"
        android:inputType="textNoSuggestions" />

    <TextView style="@style/TAAM_HeaderText" android:text="Password" android:layout_gravity="start" />
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp">

        <EditText
            android:id="@+id/signupPasswordInput"
            style="@style/TAAM_EditText"
            android:autofillHints="newPassword"
            android:importantForAutofill="yes"
            android:hint="Create a password"
            android:inputType="textPassword"
            android:paddingEnd="48dp" />

        <ImageButton
            android:id="@+id/toggleSignupPassword"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_visibility_off"
            app:tint="@color/text_secondary"
            android:contentDescription="Toggle Password Visibility" />
    </RelativeLayout>

    <TextView style="@style/TAAM_HeaderText" android:text="Confirm Password" android:layout_gravity="start" />
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="24dp">

        <EditText
            android:id="@+id/signupConfirmPasswordInput"
            style="@style/TAAM_EditText"
            android:autofillHints="newPassword"
            android:importantForAutofill="yes"
            android:hint="Re-type your password"
            android:inputType="textPassword"
            android:paddingEnd="48dp" />

        <ImageButton
            android:id="@+id/toggleSignupConfirmPassword"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_visibility_off"
            app:tint="@color/text_secondary"
            android:contentDescription="Toggle Password Visibility" />
    </RelativeLayout>

    <Button
        android:id="@+id/signupButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:padding="16dp"
        android:text="Create Account"
        android:textStyle="bold"
        app:backgroundTint="@color/button_primary"
        android:textColor="@color/white" />

    <Button
        android:id="@+id/backToLoginButton"
        style="?attr/materialButtonOutlinedStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Back to Login"
        android:textColor="@color/accent_action"
        app:strokeColor="@color/accent_action" />

</LinearLayout>
